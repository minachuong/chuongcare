<p id="notice"><%= notice %></p>

<h1>Care Receivers</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <!-- <% @care_receivers.each do |care_receiver| %>
      <tr>
        <td><%= care_receiver.first_name + " " + care_receiver.last_name %></td>
        <td><%= link_to 'Show', care_receiver %></td>
        <td><%= link_to 'Edit', edit_care_receiver_path(care_receiver) %></td>
        <td><%= link_to 'Destroy', care_receiver, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %> -->
  </tbody>
</table>

<br>

<%= link_to 'New Care Receiver', new_care_receiver_path %>
