<%= form_with(model: careship, local: true) do |form| %>
  <% if careship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(careship.errors.count, "error") %> prohibited this careship from being saved:</h2>

      <ul>
      <% careship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
